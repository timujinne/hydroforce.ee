# 🔍 Диагностика проблемы "Заявки HC не появляются"

## Проблема
Пункт меню **"HC Requests"** не появляется в админке WordPress после добавления кода.

---

## ✅ Чек-лист диагностики

### 1️⃣ Проверьте кодировку файла

**Проблема:** Ваш `functions.php` сохранён в неправильной кодировке, из-за чего кириллица превращается в `?????? HC`

**Решение:**

1. Откройте `functions.php` в редакторе (VS Code, Notepad++, Sublime)
2. **Сохраните файл как UTF-8 БЕЗ BOM**
   - VS Code: `File → Save with Encoding → UTF-8`
   - Notepad++: `Encoding → Convert to UTF-8 without BOM → Save`
3. Проверьте, что кириллица отображается правильно

---

### 2️⃣ Проверьте путь к файлу

**Убедитесь, что редактируете правильный файл:**

```
wp-content/
└── themes/
    └── your-child-theme/        ← Должна быть ДОЧЕРНЯЯ тема
        └── functions.php         ← Этот файл редактируйте
```

**НЕ РЕДАКТИРУЙТЕ родительскую тему!**

---

### 3️⃣ Включите режим отладки WordPress

Добавьте в `wp-config.php` (в корне сайта):

```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

Затем проверьте файл `wp-content/debug.log` на ошибки.

---

### 4️⃣ Проверьте синтаксис PHP

**Выполните проверку синтаксиса через SSH или хостинг-панель:**

```bash
php -l /path/to/functions.php
```

Если есть синтаксические ошибки, они будут показаны.

---

### 5️⃣ Используйте упрощённую версию для теста

**Замените весь `functions.php` на минимальный тест:**

```php
<?php
// ТЕСТОВАЯ ВЕРСИЯ - Минимальный код для проверки

function test_hydraulic_cpt() {
    register_post_type('hc_test', array(
        'labels' => array(
            'name' => 'Test HC',
            'menu_name' => 'TEST HC'
        ),
        'public' => false,
        'show_ui' => true,
        'show_in_menu' => true,
        'menu_icon' => 'dashicons-clipboard',
    ));
}
add_action('init', 'test_hydraulic_cpt');
```

**Сохраните и проверьте:**
- Обновите админку (F5)
- Если появился пункт **"TEST HC"** → Проблема в основном коде
- Если НЕ появился → Проблема с файлом или кешем

---

### 6️⃣ Очистите кеш

**Очистите все типы кеша:**

1. **Кеш WordPress:**
   - WP Admin → Settings → Permalinks → Save Changes

2. **Кеш плагинов:**
   - WP Rocket, W3 Total Cache, WP Super Cache и т.д.
   - Найдите кнопку "Clear Cache" и нажмите

3. **Кеш браузера:**
   - Ctrl+Shift+R (жёсткая перезагрузка)
   - Или Ctrl+F5

4. **Кеш хостинга:**
   - Если используется CloudFlare, Varnish и т.д.
   - Очистите кеш в панели хостинга

---

### 7️⃣ Проверьте права доступа

**Убедитесь, что у вашего пользователя есть права:**

1. Зайдите в админку как **администратор**
2. Проверьте роль: Users → Your Profile → Role должно быть **Administrator**

---

### 8️⃣ Проверьте конфликты с плагинами

**Временно отключите ВСЕ плагины:**

1. Переименуйте папку `/wp-content/plugins/` в `/wp-content/plugins-off/`
2. Обновите админку
3. Проверьте, появился ли пункт меню
4. Если появился → один из плагинов конфликтует
5. Включите плагины по одному, чтобы найти виновника

---

### 9️⃣ Проверьте, регистрируется ли тип контента

**Добавьте этот код в `functions.php` для диагностики:**

```php
add_action('init', function() {
    error_log('=== CHECKING POST TYPES ===');
    $post_types = get_post_types(array(), 'objects');
    foreach ($post_types as $pt) {
        error_log('Post Type: ' . $pt->name . ' | Menu: ' . $pt->show_in_menu);
    }
}, 999);
```

Проверьте `wp-content/debug.log`:
- Ищите строку с вашим типом контента (`hc_request` или `hydraulic_request`)
- Проверьте, что `show_in_menu` = `1` (или `true`)

---

### 🔟 Используйте финальную версию

Я создал **упрощённую и исправленную версию**: [`functions-FINAL-UTF8.php`](e:\Shtim\Downloads\Motors\functions-FINAL-UTF8.php)

**Что изменено:**

1. ✅ Удалена сломанная кириллица
2. ✅ Упрощены названия функций
3. ✅ Изменено имя post type на `hc_request` (короче, проще)
4. ✅ Убраны лишние проверки прав доступа
5. ✅ Добавлена позиция меню (`menu_position: 20`)
6. ✅ Код сохранён в UTF-8 без BOM

**Как использовать:**

1. Сделайте резервную копию текущего `functions.php`
2. Замените содержимое на код из `functions-FINAL-UTF8.php`
3. Сохраните (обязательно UTF-8 без BOM)
4. Обновите админку

---

## 🎯 Быстрая проверка (30 секунд)

Выполните эти 3 действия по порядку:

```
1. WP Admin → Settings → Permalinks → Save Changes
2. Ctrl + Shift + R (жёсткая перезагрузка браузера)
3. F5 (обновить страницу админки)
```

Если не помогло → переходите к полной диагностике выше.

---

## 📸 Как должно выглядеть

### В меню админки:
```
Dashboard
Posts
Media
Pages
Comments
HC Requests  📋    ← ЭТОТ ПУНКТ ДОЛЖЕН БЫТЬ ЗДЕСЬ
  All HC Requests
  Add New
  Settings
Appearance
Plugins
```

---

## 🆘 Если ничего не помогло

### Вариант А: Используйте плагин

Установите плагин **Custom Post Type UI**:

1. Plugins → Add New
2. Поиск: "Custom Post Type UI"
3. Install & Activate
4. CPT UI → Add/Edit Post Types
5. Создайте новый тип:
   - Post Type Slug: `hc_request`
   - Plural Label: `HC Requests`
   - Singular Label: `HC Request`
   - Menu Icon: `dashicons-clipboard`
6. Save

### Вариант Б: Проверьте хостинг

Некоторые хостинги кешируют PHP-файлы:
- Перезапустите PHP-FPM (если есть доступ)
- Подождите 5-10 минут
- Обновите страницу

### Вариант В: Проверьте версию PHP

Минимальная версия PHP: **7.4+**

Проверить: WP Admin → Tools → Site Health → Info → Server

---

## 📝 Логи для отправки разработчику

Если проблема не решается, соберите эту информацию:

1. **Версия WordPress:** WP Admin → Dashboard → версия в футере
2. **Активная тема:** Appearance → Themes → активная тема
3. **Список плагинов:** Plugins → Installed Plugins (сделайте скриншот)
4. **Содержимое debug.log:** `wp-content/debug.log` (последние 50 строк)
5. **Результат проверки синтаксиса:** `php -l functions.php`
6. **Скриншот меню админки**

---

## ✅ Решение найдено?

Если пункт меню появился:

1. ✅ Удалите тестовый код (если добавляли)
2. ✅ Верните финальную версию кода
3. ✅ Проверьте, что форма работает
4. ✅ Протестируйте отправку заявки

---

**Файлы для использования:**

- [`functions-FINAL-UTF8.php`](e:\Shtim\Downloads\Motors\functions-FINAL-UTF8.php) - Финальная рабочая версия
- [`Form_hc_en.html`](e:\Shtim\Downloads\Motors\Form_hc_en.html) - Английская форма
- [`INSTALLATION_INSTRUCTIONS.md`](e:\Shtim\Downloads\Motors\INSTALLATION_INSTRUCTIONS.md) - Подробная инструкция
