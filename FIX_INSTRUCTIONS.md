# 🔧 Как исправить проблему с "Заявки HC"

## Проблема
В WordPress админке не появляется пункт меню **"Заявки HC"**

## Причина
В вашем `functions.php` не хватает регистрации **Custom Post Type**. Это именно та функция, которая создаёт пункт меню в админке.

---

## ✅ Решение

### Вариант 1: Замените весь код

1. Откройте файл: **`wp-content/themes/your-child-theme/functions.php`**

2. **Замените всё содержимое** на код из файла: **`functions-corrected.php`**

3. Сохраните файл

4. Обновите страницу админки WordPress

5. **Готово!** Теперь в меню слева должен появиться пункт **"Заявки HC"** с иконкой блокнота

---

### Вариант 2: Добавьте недостающий код (если хотите сохранить оригинальный файл)

Найдите в вашем `functions.php` строку:
```php
add_shortcode('contact_form', 'form_shortcode');
```

**Сразу после неё** добавьте этот блок:

```php
/**
 * CUSTOM POST TYPE - Hydraulic Requests
 */
function hydroforce_register_hydraulic_requests_cpt() {
    $labels = array(
        'name'               => 'Hydraulic Requests',
        'singular_name'      => 'Request',
        'menu_name'          => 'Заявки HC',
        'add_new'            => 'Add New',
        'add_new_item'       => 'Add New Request',
        'edit_item'          => 'Edit Request',
        'new_item'           => 'New Request',
        'view_item'          => 'View Request',
        'search_items'       => 'Search Requests',
        'not_found'          => 'No requests found',
        'not_found_in_trash' => 'No requests found in trash',
    );

    $args = array(
        'labels'              => $labels,
        'public'              => false,
        'show_ui'             => true,
        'show_in_menu'        => true,
        'menu_icon'           => 'dashicons-clipboard',
        'capability_type'     => 'post',
        'hierarchical'        => false,
        'supports'            => array('title', 'editor'),
        'has_archive'         => false,
        'rewrite'             => false,
        'query_var'           => false,
        'show_in_rest'        => false,
    );

    register_post_type('hydraulic_request', $args);
}
add_action('init', 'hydroforce_register_hydraulic_requests_cpt');
```

---

## 🧪 Проверка

После сохранения файла:

1. Обновите админку WordPress (F5)
2. В левом меню должен появиться пункт: **"Заявки HC"** 📋
3. Кликните на него - откроется список заявок (пока пустой)

---

## 📍 Что делает этот код?

```php
register_post_type('hydraulic_request', $args);
```

Эта функция регистрирует **новый тип контента** в WordPress:
- Название: `hydraulic_request`
- Отображаемое имя в меню: `Заявки HC`
- Иконка: блокнот (clipboard)
- Видимость: только в админке (не на сайте)

---

## 🔄 Если не помогло

### 1. Очистите кеш WordPress
```
WP Admin → Settings → Permalinks → Save Changes (просто нажмите "Сохранить")
```

### 2. Проверьте ошибки PHP
Откройте файл `wp-config.php` и добавьте:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

Затем проверьте файл: `wp-content/debug.log`

### 3. Убедитесь, что нет дубликатов функций
Поищите в файле `functions.php`:
```php
function hydroforce_register_hydraulic_requests_cpt()
```

Должна быть **ТОЛЬКО ОДНА** такая функция!

---

## 📸 Как должно выглядеть

### В меню админки:
```
┌─────────────────────┐
│ Dashboard           │
│ Posts               │
│ Media               │
│ Pages               │
│ Comments            │
│ Заявки HC  📋       │ ← ВОТ ЭТО ДОЛЖНО ПОЯВИТЬСЯ
│   All Requests      │
│   Add New           │
│   Settings          │
└─────────────────────┘
```

### При клике на "Заявки HC":
Откроется таблица со столбцами:
- Company
- Contact Person
- Email
- Phone
- Cylinder Type
- Date

---

## 💡 Дополнительно

### Изменить название в меню

Если хотите изменить `Заявки HC` на другое название, измените строку:

```php
'menu_name' => 'Заявки HC',  // ← Здесь
```

Например:
```php
'menu_name' => 'HC Requests',
'menu_name' => 'Цилиндры',
'menu_name' => 'Заявки',
```

### Изменить иконку

Доступные иконки: https://developer.wordpress.org/resource/dashicons/

```php
'menu_icon' => 'dashicons-clipboard',  // ← Здесь
```

Примеры:
- `dashicons-email` - конверт
- `dashicons-admin-tools` - гаечный ключ
- `dashicons-feedback` - речевой пузырь
- `dashicons-businessman` - человек

---

## ✅ Итоговый чек-лист

- [ ] Код добавлен в `functions.php`
- [ ] Файл сохранён
- [ ] Админка обновлена (F5)
- [ ] Пункт "Заявки HC" появился в меню
- [ ] При клике открывается список заявок
- [ ] Внутри есть кнопка "Add New" и "Settings"

---

**Готово!** Теперь форма будет сохранять заявки в этот раздел. 🎉
