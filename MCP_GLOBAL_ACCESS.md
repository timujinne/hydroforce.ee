# 🌍 Глобальный доступ к MCP серверам в Claude Code

## ✅ Хорошая новость!

MCP серверы УЖЕ настроены глобально в конфигурации:
```
C:\Users\Shtim\.claude.json → mcpServers (верхний уровень)
```

Это означает, что серверы доступны для ВСЕХ проектов!

## 🔍 Проверка глобальной доступности

### Из любой директории выполните:
```bash
node "C:\Users\Shtim\.vscode\extensions\anthropic.claude-code-2.0.15\resources\claude-code\cli.js" mcp list
```

Вы увидите все глобальные серверы:
- ✓ filesystem
- ✓ sequential-thinking
- ✓ Context7
- ✓ chrome-devtools
- ✓ mcp-server-time
- ✗ github (не подключается)

## 🤔 Почему MCP серверы не видны в новом проекте VSCode?

### Причина: Требуется одобрение (approval)

Когда вы открываете новый проект в Claude Code, появляется диалог с вопросом:
> "Do you want to enable MCP servers for this project?"

Нужно **одобрить** использование глобальных MCP серверов в конкретном проекте.

## 📝 Решение: Два способа

### Способ 1: Через UI VSCode (РЕКОМЕНДУЕТСЯ)

1. Откройте новый проект в VSCode
2. Запустите Claude Code
3. Когда появится диалог про MCP серверы - нажмите **"Enable"** или **"Approve"**
4. Серверы станут доступны в этом проекте

### Способ 2: Через конфигурацию вручную

Если диалог не появляется или вы его пропустили:

1. Откройте Claude Code в новом проекте
2. Откройте **"Manage MCP Servers"** (через меню или команду)
3. Вы увидите список глобальных MCP серверов
4. Включите нужные серверы для проекта

### Способ 3: Сбросить настройки проекта (если ничего не помогает)

Удалить настройки проекта, чтобы диалог появился снова:

```bash
node "C:\Users\Shtim\.vscode\extensions\anthropic.claude-code-2.0.15\resources\claude-code\cli.js" mcp reset-project-choices
```

Эта команда сбрасывает все одобренные/отклоненные MCP серверы для текущего проекта.

## 🔬 Проверка конфигурации проекта

### Текущая структура .claude.json:

```json
{
  "mcpServers": {
    // ГЛОБАЛЬНЫЕ серверы (доступны везде)
    "filesystem": { ... },
    "sequential-thinking": { ... },
    "chrome-devtools": { ... }
  },
  "projects": {
    "e:\\Shtim\\Downloads\\Motors": {
      "mcpServers": {},  // Пусто - использует глобальные
      "enabledMcpjsonServers": [],   // Одобренные для этого проекта
      "disabledMcpjsonServers": []   // Отклоненные для этого проекта
    }
  }
}
```

### Что означают поля:

- **`mcpServers`** (верхний уровень) - глобальные серверы для всех проектов
- **`projects.*.mcpServers`** - специфичные для проекта серверы (переопределяют глобальные)
- **`enabledMcpjsonServers`** - список одобренных глобальных серверов для проекта
- **`disabledMcpjsonServers`** - список отклоненных глобальных серверов для проекта

## 🎯 Автоматическое одобрение для новых проектов

К сожалению, в текущей версии Claude Code НЕТ способа автоматически одобрять все глобальные MCP серверы для новых проектов.

Вам придется одобрять их вручную для каждого нового проекта через UI.

## 🔧 Workaround: Предварительное одобрение через конфигурацию

Если вы хотите, чтобы серверы были автоматически доступны в конкретном проекте, можно вручную добавить их в `enabledMcpjsonServers`:

**⚠️ ВНИМАНИЕ: Редактирование вручную не рекомендуется, используйте только если понимаете что делаете!**

```json
{
  "projects": {
    "e:\\Shtim\\Downloads\\YourNewProject": {
      "enabledMcpjsonServers": [
        "filesystem",
        "sequential-thinking",
        "Context7",
        "chrome-devtools",
        "mcp-server-time"
      ],
      "mcpServers": {},
      "allowedTools": []
    }
  }
}
```

## 🚀 Быстрая проверка в новом проекте

### 1. Откройте новый проект в VSCode
### 2. Запустите в терминале:

```bash
.\add-mcp-server.ps1 list
```

Или:

```bash
node "C:\Users\Shtim\.vscode\extensions\anthropic.claude-code-2.0.15\resources\claude-code\cli.js" mcp list
```

### 3. Если серверы видны в CLI, но не в UI VSCode:

- Проверьте диалоги Claude Code про одобрение MCP серверов
- Откройте "Manage MCP Servers" и включите серверы вручную
- Используйте `mcp reset-project-choices` чтобы сбросить выбор

## 📊 Уровни доступа MCP серверов

### 1. Глобальный уровень (✅ У ВАС НАСТРОЕНО)
```
C:\Users\Shtim\.claude.json → mcpServers
```
**Плюсы:**
- Доступны во всех проектах
- Настраиваются один раз
- Легко управлять через CLI

**Минусы:**
- Требуют одобрения для каждого нового проекта через UI

### 2. Project-specific уровень
```
C:\Users\Shtim\.claude.json → projects.*.mcpServers
```
**Плюсы:**
- Автоматически доступны в проекте без одобрения
- Можно переопределить глобальные настройки

**Минусы:**
- Нужно настраивать для каждого проекта отдельно

### 3. Local .mcp.json (в репозитории проекта)
```
E:\Shtim\Downloads\YourProject\.mcp.json
```
**Плюсы:**
- Можно коммитить в Git для команды
- Автоматически загружаются для всех участников

**Минусы:**
- Требуют одобрения от каждого пользователя
- Могут содержать секреты (требуют осторожности)

## 💡 Рекомендация

**Ваш текущий подход (глобальные серверы) - ПРАВИЛЬНЫЙ!**

Просто **одобряйте серверы через UI** когда открываете новый проект в первый раз:
1. Claude Code покажет диалог "Enable MCP servers?"
2. Нажмите "Enable" или "Approve"
3. Готово!

Это делается **один раз для каждого проекта** и сохраняется в `.claude.json`.

## 🔍 Troubleshooting

### Проблема: Диалог одобрения не появляется

**Решение:**
```bash
cd "your-new-project"
node "C:\Users\Shtim\.vscode\extensions\anthropic.claude-code-2.0.15\resources\claude-code\cli.js" mcp reset-project-choices
```

### Проблема: Серверы видны в CLI, но не в VSCode UI

**Решение:**
1. Перезапустите VSCode
2. Откройте "Manage MCP Servers"
3. Проверьте, не отключены ли серверы вручную

### Проблема: Нужно одобрить сразу для многих проектов

**Решение:** К сожалению, автоматизации нет. Нужно одобрять вручную для каждого проекта.

## 📚 Проверка статуса серверов

### Из любого проекта:
```powershell
.\add-mcp-server.ps1 list
```

### Из CLI:
```bash
node "C:\Users\Shtim\.vscode\extensions\anthropic.claude-code-2.0.15\resources\claude-code\cli.js" mcp list
```

### Ожидаемый результат:
```
Checking MCP server health...

filesystem: npx @modelcontextprotocol/server-filesystem E:\Shtim\Downloads - ✓ Connected
sequential-thinking: npx @modelcontextprotocol/server-sequential-thinking - ✓ Connected
Context7: npx -y @upstash/context7-mcp@latest - ✓ Connected
chrome-devtools: npx -y chrome-devtools-mcp@latest - ✓ Connected
mcp-server-time: python -m mcp_server_time --local-timezone=Europe/Tallinn - ✓ Connected
```

---

## ✅ Итого

**MCP серверы УЖЕ настроены глобально!**

Чтобы использовать их в новом проекте:
1. Откройте проект в VSCode с Claude Code
2. Одобрите серверы когда появится диалог
3. Или включите их вручную в "Manage MCP Servers"

Это нужно сделать **один раз для каждого нового проекта**.

После одобрения серверы будут доступны в этом проекте всегда!
