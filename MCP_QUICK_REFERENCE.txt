════════════════════════════════════════════════════════════════
  БЫСТРЫЙ СПРАВОЧНИК: Настройка MCP в Claude Code для VSCode
════════════════════════════════════════════════════════════════

█ В ЧЕМ БЫЛА ПРОБЛЕМА?
══════════════════════════════════════════════════════════════

Claude Code в VSCode НЕ использует:
  ❌ claude_desktop_config.json (только для Claude Desktop)
  ❌ mcp.json в VSCode/User (для GitHub Copilot)
  ❌ settings.json с claude-code.mcpServers (не работает)

✅ ПРАВИЛЬНОЕ РЕШЕНИЕ: Использовать CLI инструмент Claude Code

Конфигурация сохраняется в: C:\Users\Shtim\.claude.json


█ ПОЧЕМУ ТАК СЛОЖНО?
══════════════════════════════════════════════════════════════

1. Разные продукты = разные форматы конфигурации:
   • Claude Desktop (приложение) → свой формат
   • Claude Code VSCode (расширение) → свой формат через CLI
   • GitHub Copilot → свой формат

2. Документация не показывает полный путь к CLI

3. Windows особенности (пути, WSL, экранирование)

4. Три уровня scope (user/local/project) - не очевидно какой выбрать


█ КАК ДОБАВИТЬ MCP СЕРВЕР?
══════════════════════════════════════════════════════════════

┌─ Способ 1: Используйте helper скрипт (РЕКОМЕНДУЕТСЯ) ───────┐
│                                                               │
│  PowerShell:                                                  │
│  .\add-mcp-server.ps1 list                                    │
│  .\add-mcp-server.ps1 add server-name npx -y package-name    │
│  .\add-mcp-server.ps1 remove server-name                      │
│  .\add-mcp-server.ps1 get server-name                         │
│                                                               │
│  CMD:                                                         │
│  add-mcp-server.bat list                                      │
│  add-mcp-server.bat add server-name -- npx -y package-name   │
│  add-mcp-server.bat remove server-name                        │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌─ Способ 2: Прямое использование CLI ────────────────────────┐
│                                                               │
│  SET CLAUDE_CLI=C:\Users\Shtim\.vscode\extensions\          │
│    anthropic.claude-code-2.0.15\resources\claude-code\cli.js │
│                                                               │
│  node "%CLAUDE_CLI%" mcp add --scope user --transport stdio  │
│    server-name -- npx -y package-name                         │
│                                                               │
└───────────────────────────────────────────────────────────────┘


█ СТРУКТУРА КОМАНДЫ
══════════════════════════════════════════════════════════════

node cli.js mcp add \
  --scope user           ← user (все проекты) или local (текущий)
  --transport stdio      ← stdio, http, или sse
  server-name            ← любое имя для сервера
  --                     ← разделитель (ВАЖНО!)
  npx -y package-name    ← команда запуска сервера


█ ПОПУЛЯРНЫЕ ПРИМЕРЫ
══════════════════════════════════════════════════════════════

# Filesystem (доступ к файлам)
.\add-mcp-server.ps1 add filesystem npx @modelcontextprotocol/server-filesystem "E:\Shtim\Downloads"

# Sequential Thinking (пошаговое мышление)
.\add-mcp-server.ps1 add sequential-thinking npx @modelcontextprotocol/server-sequential-thinking

# Chrome DevTools (отладка браузера)
.\add-mcp-server.ps1 add chrome-devtools npx -y chrome-devtools-mcp@latest

# Brave Search (веб-поиск)
.\add-mcp-server.ps1 add brave-search npx -y @modelcontextprotocol/server-brave-search

# Git (интеграция с Git)
.\add-mcp-server.ps1 add git npx -y @modelcontextprotocol/server-git

# GitHub (интеграция с GitHub)
.\add-mcp-server.ps1 add github npx -y @modelcontextprotocol/server-github

# PostgreSQL (подключение к БД)
.\add-mcp-server.ps1 add postgres npx -y @modelcontextprotocol/server-postgres

# Python сервер (например, time)
.\add-mcp-server.ps1 add mcp-server-time python -m mcp_server_time --local-timezone=Europe/Tallinn


█ УПРАВЛЕНИЕ СЕРВЕРАМИ
══════════════════════════════════════════════════════════════

# Показать все серверы и их статус (работает из любой директории!)
.\add-mcp-server.ps1 list

# Удалить сервер
.\add-mcp-server.ps1 remove server-name

# Детали конкретного сервера
.\add-mcp-server.ps1 get server-name

# Сбросить одобренные серверы для текущего проекта
node cli.js mcp reset-project-choices


█ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ (API ключи, токены)
══════════════════════════════════════════════════════════════

node "%CLAUDE_CLI%" mcp add --scope user --transport stdio server-name \
  --env API_KEY=your_key \
  --env TOKEN=your_token \
  -- npx -y package-name


█ ТРИ УРОВНЯ SCOPE
══════════════════════════════════════════════════════════════

--scope user      ✅ Рекомендуется - серверы доступны везде
--scope local        Только для текущей директории
--scope project      Только для текущего VSCode workspace


█ ТРИ ТИПА ТРАНСПОРТА
══════════════════════════════════════════════════════════════

--transport stdio    ✅ Самый частый (npx, python, node)
--transport http        Для HTTP API серверов
--transport sse         Для Server-Sent Events


█ ПОИСК НОВЫХ MCP СЕРВЕРОВ
══════════════════════════════════════════════════════════════

• GitHub: https://github.com/modelcontextprotocol/servers
• MCP Registry: https://mcp-registry.org/
• NPM: поиск по "mcp-server" или "@modelcontextprotocol/server-*"
• Awesome MCP: https://github.com/punkpeye/awesome-mcp-servers


█ ВАЖНЫЕ ЗАМЕЧАНИЯ
══════════════════════════════════════════════════════════════

⚠️  Путь к CLI содержит номер версии расширения (2.0.15)
    При обновлении расширения путь изменится!

💡  Найти актуальную версию:
    code --locate-extension anthropic.claude-code

🔄  Серверы активируются сразу без перезапуска VSCode

📝  НЕ редактируйте .claude.json вручную - используйте CLI

🌍  ГЛОБАЛЬНЫЙ ДОСТУП:
    Серверы УЖЕ настроены глобально!
    Для нового проекта: одобрите серверы в диалоге VSCode
    Или: откройте "Manage MCP Servers" и включите вручную


█ TROUBLESHOOTING
══════════════════════════════════════════════════════════════

Проблема: "No MCP servers configured"
Решение: Убедитесь, что используете --scope user

Проблема: Сервер не запускается
Решение: Проверьте команду вручную (npx -y package-name)

Проблема: "✗ Failed to connect"
Решение: Проверьте логи в VSCode Output → Claude Code


█ ТЕКУЩИЕ УСТАНОВЛЕННЫЕ СЕРВЕРЫ
══════════════════════════════════════════════════════════════

✅ filesystem        - доступ к E:\Shtim\Downloads
✅ sequential-thinking - пошаговое мышление
✅ Context7          - контекстная память
✅ chrome-devtools   - отладка браузера
✅ mcp-server-time   - работа со временем (Europe/Tallinn)
⚠️ github            - не подключен (требует настройки)


█ ФАЙЛЫ В ПРОЕКТЕ
══════════════════════════════════════════════════════════════

add-mcp-server.ps1      - PowerShell скрипт (рекомендуется)
add-mcp-server.bat      - CMD batch скрипт
MCP_GUIDE.md            - Полное руководство на русском
MCP_QUICK_REFERENCE.txt - Этот файл (краткий справочник)


════════════════════════════════════════════════════════════════
  Для подробной информации см. MCP_GUIDE.md
════════════════════════════════════════════════════════════════
