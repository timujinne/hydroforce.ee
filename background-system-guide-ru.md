# Система Фоновых Изображений для Hydroforce

## Краткая Инструкция

Вместо написания всех стилей inline, вы можете использовать **CSS Custom Properties** для передачи только URL изображения.

---

## 🎯 Рекомендуемый Метод: CSS Custom Properties

### Как Использовать

```html
<section class="hero-bg" style="--bg-image: url('путь/к/изображению.jpg');">
    <div class="container">
        <div class="hero-content">
            <h1>Заголовок</h1>
            <p>Текст описания</p>
            <a href="#" class="btn">Кнопка</a>
        </div>
    </div>
</section>
```

### Почему Это Лучше?

**❌ Плохо (много inline стилей):**
```html
<div style="background-image: linear-gradient(rgba(2,24,34,0.8), rgba(1,87,125,0.1)), url('image.jpg'); background-size: cover; background-position: center; background-repeat: no-repeat; background-attachment: fixed;">
```

**✅ Хорошо (один inline стиль):**
```html
<div class="hero-bg" style="--bg-image: url('image.jpg');">
```

Все стили (gradient, размер, позиция) остаются в CSS файле!

---

## 📋 Доступные Классы

### 1. `.hero-bg` - Стандартный Hero
Темный градиент от верха к низу
```html
<section class="hero-bg" style="--bg-image: url('image.jpg');">
```

**Градиент:** `rgba(2, 24, 34, 0.8)` → `rgba(1, 87, 125, 0.1)`

---

### 2. `.hero-bg-dark` - Очень Темный Hero
Для максимальной читаемости текста
```html
<section class="hero-bg-dark" style="--bg-image: url('image.jpg');">
```

**Градиент:** `rgba(2, 24, 34, 0.85)` → `rgba(0, 58, 82, 0.85)`

---

### 3. `.hero-bg-light` - Светлый Hero
Со светлым оверлеем для темного текста
```html
<section class="hero-bg-light" style="--bg-image: url('image.jpg');">
```

**Градиент:** `rgba(248, 249, 250, 0.85)` → `rgba(233, 236, 239, 0.85)`

**⚠️ Внимание:** Используйте темный текст!

---

### 4. `.hero-bg-gradient-lr` - Градиент Слева Направо
Идеально для контента слева, изображение видно справа
```html
<section class="hero-bg-gradient-lr" style="--bg-image: url('image.jpg');">
```

**Градиент:** Синий слева → прозрачный справа

---

### 5. `.hero-bg-gradient-rl` - Градиент Справа Налево
Для контента справа
```html
<section class="hero-bg-gradient-rl" style="--bg-image: url('image.jpg');">
```

**Градиент:** Прозрачный слева → синий справа

---

### 6. `.cta-bg` - CTA Section с Красным Оверлеем
Для призывов к действию
```html
<section class="cta-bg" style="--bg-image: url('image.jpg');">
```

**Градиент:** `rgba(188, 54, 54, 0.9)` → `rgba(150, 42, 42, 0.9)`

---

## 💡 Примеры Использования

### Пример 1: Главный Hero
```html
<section class="hero-bg" style="--bg-image: url('https://www.hydroforce.ee/wp-content/uploads/2025/10/hero.webp');">
    <div class="container">
        <div class="hero-content">
            <h1>Hydroforce Solutions</h1>
            <p>Профессиональные гидравлические системы</p>
            <a href="#products" class="btn">Наши продукты</a>
        </div>
    </div>
</section>
```

### Пример 2: Страница Продукта
```html
<section class="hero-bg-dark" style="--bg-image: url('product-image.jpg');">
    <div class="container">
        <h1>Название Продукта</h1>
        <p>Описание продукта</p>
    </div>
</section>
```

### Пример 3: CTA Блок
```html
<section class="cta-bg" style="--bg-image: url('cta-background.jpg');">
    <div class="container">
        <h2>Готовы начать?</h2>
        <p>Свяжитесь с нами сегодня</p>
        <a href="#contact" class="btn btn-white">Связаться</a>
    </div>
</section>
```

---

## 🔄 Альтернативный Метод: Data-Атрибут

Если вам нужен полностью чистый HTML без inline стилей:

### HTML
```html
<section class="hero-auto-bg" data-bg-image="https://example.com/image.jpg">
    <div class="container">
        <h1>Заголовок</h1>
    </div>
</section>
```

### JavaScript (добавьте в конец страницы)
```javascript
document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('[data-bg-image]').forEach(element => {
        const bgImage = element.getAttribute('data-bg-image');
        if (bgImage) {
            element.style.backgroundImage = `url('${bgImage}')`;
        }
    });
});
```

### CSS
```css
.hero-auto-bg {
    position: relative;
    min-height: 400px;
    background-size: cover;
    background-position: center;
    color: white;
    padding: 6rem 0;
}

.hero-auto-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(rgba(2, 24, 34, 0.8), rgba(1, 87, 125, 0.1));
    z-index: 0;
}

.hero-auto-bg > * {
    position: relative;
    z-index: 1;
}
```

---

## 📊 Сравнение Методов

| Характеристика | CSS Variables | Data-Атрибут |
|----------------|---------------|---------------|
| **Inline стили** | Один (только переменная) | Нет |
| **JavaScript** | Не нужен | Требуется |
| **Производительность** | ⚡ Отлично | ⚠️ Небольшая задержка |
| **SEO** | ✅ Идеально | ✅ Идеально |
| **Поддержка без JS** | ✅ Да | ❌ Нет |
| **Рекомендация** | **✅ ДА** | Для особых случаев |

---

## ⚙️ Настройка Градиентов

Если вам нужны другие градиенты, вы можете создать свои классы:

```css
/* Пример: фиолетовый градиент */
.hero-bg-purple {
    background-image: 
        linear-gradient(rgba(98, 0, 234, 0.8), rgba(236, 64, 122, 0.8)),
        var(--bg-image);
    /* остальные стили как у .hero-bg */
}
```

Или изменить существующие в `hydroforce-enhanced.css`.

---

## 📱 Мобильная Версия

На мобильных устройствах `background-attachment: fixed` автоматически меняется на `scroll` для лучшей производительности.

Это происходит в медиа-запросе:
```css
@media (max-width: 768px) {
    .hero-bg,
    .hero-bg-dark,
    /* и т.д. */ {
        background-attachment: scroll;
    }
}
```

---

## ✅ Чеклист для Внедрения

- [ ] Добавьте обновленный `hydroforce-enhanced.css` на сайт
- [ ] Замените inline стили фонов на классы `.hero-bg`
- [ ] Проверьте читаемость текста на всех секциях
- [ ] Протестируйте на мобильных устройствах
- [ ] Убедитесь, что изображения оптимизированы (WebP формат)
- [ ] Проверьте скорость загрузки страниц

---

## 🎨 Рекомендации по Изображениям

### Размеры
- **Desktop:** минимум 1920x1080px
- **Mobile:** минимум 1080x1920px (vertical)

### Формат
- **WebP** - лучший выбор (меньший размер, хорошее качество)
- **JPEG** - запасной вариант

### Оптимизация
- Сжимайте изображения (TinyPNG, ImageOptim)
- Используйте lazy loading для изображений ниже первого экрана
- Добавьте placeholder цвет в CSS

---

## 🆘 Решение Проблем

### Проблема: Текст плохо читается
**Решение:** Используйте `.hero-bg-dark` или увеличьте непрозрачность градиента

### Проблема: Изображение не загружается
**Решение:** Проверьте URL, добавьте fallback цвет:
```css
.hero-bg {
    background-color: var(--primary-blue-dark);
    /* остальные стили */
}
```

### Проблема: Медленная загрузка
**Решение:** 
- Оптимизируйте изображения
- Используйте CDN
- Добавьте lazy loading

---

## 📞 Поддержка

Если возникли вопросы или нужна помощь с настройкой, посмотрите:
- `hydroforce-background-system.html` - полный демо с примерами
- `enhancement-guide.md` - подробное руководство по всем улучшениям

---

**Последнее обновление:** 2025-10-21  
**Версия CSS:** 4.0 Enhanced
